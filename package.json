{
  "name": "proyecto-gestion-hospitalaria",
  "private": true,
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:web": "npm --prefix web run dev",
    "dev:mobile": "npm --prefix mobile run start",
    "dev": "concurrently -n backend,web -c blue,green \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:tunnel": "node scripts/dev-tunnel.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
